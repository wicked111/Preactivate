<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Preactivate" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/background</item>
        <!-- Add these lines to customize status bar and navigation bar colors -->
        <item name="android:statusBarColor">@color/background</item>
        <item name="android:navigationBarColor">@color/background</item>
    </style>

    <style name="CustomTabLayout" parent="Widget.Design.TabLayout">
        <!-- Color for the text of the tabs -->
        <item name="tabTextColor">@color/tabcolor</item>
        <!-- Color for the selected tab -->
        <item name="tabSelectedTextColor">@color/white</item>
        <!-- Color for the indicator under the selected tab -->
        <item name="tabIndicatorColor">@color/tabcolor</item>
    </style>

    <style name="Theme.Preactivate" parent="Base.Theme.Preactivate" />
</resources>
